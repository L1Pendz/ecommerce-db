# Ecommerce DB API

A TypeScript Express API connected to PostgreSQL for managing customers, products, orders, order items, vendors, and supplies.

## Prerequisites

- Node.js installed
- PostgreSQL installed and running
- A PostgreSQL database with the required tables created

## Installation

Clone the repository and install dependencies:

```bash
npm install
```
